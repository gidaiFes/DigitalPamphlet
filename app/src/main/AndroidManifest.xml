<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kumazawakento.digitalpamphlet">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gidai"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:uiOptions="splitActionBarWhenNarrow">
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gainTestActivity" />
        <activity android:name=".sendaiTestActivity" />
        <activity android:name=".kentTestActivity"></activity>
    </application>

</manifest>